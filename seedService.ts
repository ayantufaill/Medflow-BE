import { adminFinanceService } from './src/services/admin-finance.service.js'; const data = ['Write Off', 'Un-Collected', 'Pre Payment', 'Wellness', 'Small Balance W/O', 'Curtsey W/O', 'Non Payment']; async function main() { for (let i = 0; i < data.length; i++) { await adminFinanceService.createDefinition(1, { name: data[i], note: '[credit]', itemOrder: i + 20 }); console.log('Created ' + data[i]); } } main();
