import { PrismaClient } from '@prisma/client'; const prisma = new PrismaClient(); async function main() { const names = ['Write Off', 'Un-Collected', 'Pre Payment', 'Wellness', 'Small Balance W/O', 'Curtsey W/O', 'Non Payment']; for (let i = 0; i < names.length; i++) { await prisma.definition.create({ data: { Category: 1, ItemOrder: i + 20, ItemName: names[i], ItemValue: '', ItemColor: 0, IsHidden: 0 } }); } console.log('Seeded 7 adjustment types'); } main();
